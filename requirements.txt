# --- เฟรมเวิร์กหลักของเว็บแอปพลิเคชัน ---
flask # เฟรมเวิร์กหลักสำหรับสร้างเว็บเซิร์ฟเวอร์และจัดการคำขอ (request)

# --- ด้านคอมพิวเตอร์วิทัศน์ (Computer Vision) และการเรียนรู้ของเครื่อง (Machine Learning) ---
opencv-contrib-python # (OpenCV) สำหรับจับภาพวิดีโอจากเว็บแคมและประมวลผลภาพเบื้องต้น
mediapipe==0.10.14 # ไลบรารีของ Google สำหรับตรวจจับจุดสำคัญบนมือ (hand landmarks) จากวิดีโอ
scikit-learn==1.7.1 # ไลบรารี Machine Learning ที่ใช้สำหรับฝึกสอนและใช้งานโมเดลการจำภาษามือ

# --- ไลบรารีหลักด้านวิทยาศาสตร์และข้อมูล (ส่วนประกอบย่อยของ ML) ---
numpy==2.2.6 # แพ็กเกจพื้นฐานสำหรับการคำนวณเชิงตัวเลข โดยเฉพาะกับอาร์เรย์ (ถูกใช้โดย OpenCV และ Scikit-learn)
pandas==2.3.1 # ใช้สำหรับการจัดการและวิเคราะห์ข้อมูล; มักจะใช้ในขั้นตอนการฝึกสอนโมเดล
scipy==1.15.3 # ไลบรารีสำหรับการคำนวณทางวิทยาศาสตร์; เป็นส่วนประกอบหลักของ scikit-learn

# --- ด้านรูปภาพและการแสดงผล ---
pillow==11.3.0 # (PIL) สำหรับเปิด, แก้ไข, และบันทึกไฟล์รูปภาพ
matplotlib==3.10.3 # ใช้สำหรับสร้างกราฟและการแสดงผลข้อมูล, มักใช้ในการวิเคราะห์โมเดลหรือดีบักโค้ด

# --- ส่วนประกอบย่อยของโมเดล Machine Learning ---
joblib==1.5.1 # มักถูกใช้โดย scikit-learn สำหรับบันทึก/โหลดโมเดลที่ฝึกแล้ว
threadpoolctl==3.6.0 # จัดการจำนวนเธรดเพื่อเพิ่มประสิทธิภาพในไลบรารีที่ต้องคำนวณตัวเลขเยอะๆ
absl-py==2.3.1 # ส่วนประกอบย่อยของไลบรารี Google เช่น MediaPipe
flatbuffers==25.2.10 # ส่วนประกอบย่อยของ MediaPipe
protobuf==4.25.8 # ส่วนประกอบย่อยของ MediaPipe (สำหรับจัดการโครงสร้างข้อมูล)

# --- ส่วนประกอบย่อยทั่วไปและเครื่องมืออำนวยความสะดวก ---
requests==2.32.4 # สำหรับส่ง HTTP request (กรณีที่แอปต้องเรียก API ภายนอก)
urllib3==2.5.0 # ส่วนประกอบย่อยของ requests
idna==3.10 # ส่วนประกอบย่อยของ requests
certifi==2025.7.14 # จัดหาใบรับรอง SSL เพื่อการเชื่อมต่อที่ปลอดภัยใน requests
charset-normalizer==3.4.2 # ส่วนประกอบย่อยของ requests
six==1.17.0 # ไลบรารีเพื่อให้โค้ดเข้ากันได้ระหว่าง Python 2 และ 3,จำเป็นสำหรับหลายแพ็กเกจ
packaging==25.0 # เครื่องมือหลักสำหรับจัดการแพ็กเกจและเวอร์ชันของ Python
python-dateutil==2.9.0.post0 # ส่วนขยายความสามารถสำหรับ datetime (ส่วนประกอบย่อยของ pandas)
pytz==2025.2 # ไลบรารีสำหรับจัดการโซนเวลา (ส่วนประกอบย่อยของ pandas)
tzdata==2025.2 # ข้อมูลโซนเวลา (ส่วนประกอบย่อยของ pandas)
tqdm==4.67.1 # เครื่องมือแสดงแถบความคืบหน้า, มีประโยชน์ตอนฝึกโมเดลหรือรันงานนานๆ
colorama==0.4.6 # ทำให้สามารถแสดงผลข้อความสีๆ ใน terminal ได้
pyparsing==3.2.3 # ส่วนประกอบย่อยของ matplotlib
kiwisolver==1.4.8 # ส่วนประกอบย่อยของ matplotlib
fonttools==4.59.0 # ส่วนประกอบย่อยของ matplotlib
cycler==0.12.1 # ส่วนประกอบย่อยของ matplotlib
contourpy==1.3.2 # ส่วนประกอบย่อยของ matplotlib

# --- ด้านเสียง (เพิ่มเข้ามาเพื่อดีบัก Text-to-Speech) ---
sounddevice==0.5.2 # สำหรับเล่นและอัดเสียง, เราใช้ไลบรารีนี้เพื่อทดสอบความสามารถในการพูด
cffi==1.17.1 # ส่วนประกอบย่อยของ sounddevice
pycparser==2.22 # ส่วนประกอบย่อยของ cffi

# --- เฟรมเวิร์กทางเลือก/ที่ไม่ได้ใช้ (สามารถลบออกได้สำหรับโปรเจกต์ Flask) ---
# ไลบรารีด้านล่างนี้เป็นของ Streamlit ซึ่งเป็นเฟรมเวิร์กอีกตัวสำหรับสร้างเว็บแอป
# ไลบรารีเหล่านี้ไม่ได้ถูกใช้ในแอป Flask ปัจจุบัน และสามารถลบออกได้อย่างปลอดภัย
streamlit
streamlit-webrtc
av

# --- ส่วนประกอบย่อยที่อาจไม่ได้ใช้งาน (แนะนำให้ตรวจสอบ) ---
# ไลบรารีเหล่านี้มักเป็นส่วนประกอบย่อยของไลบรารี ML อื่นๆ เช่น TensorFlow หรือ JAX,
# แต่อาจไม่จำเป็นสำหรับโปรเจกต์นี้โดยตรง
jax==0.6.2
jaxlib==0.6.2
ml_dtypes==0.5.3
opt_einsum==3.4.0
package_name==0.1 # ดูเหมือนจะเป็นชื่อแพ็กเกจตัวอย่างหรือความผิดพลาด และน่าจะลบออกได้
